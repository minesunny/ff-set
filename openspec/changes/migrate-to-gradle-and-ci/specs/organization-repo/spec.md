## ADDED Requirements

### Requirement: Private Organization Repository
The project SHALL be hosted in a private repository under the `minesunny` organization.

#### Scenario: Push to organization
- **WHEN** the repository is pushed
- **THEN** it MUST be accessible at `https://github.com/minesunny/ff-set`.
